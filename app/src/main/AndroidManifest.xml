<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ar.uba.fi.remy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".RecipesCookedActivity"
            android:theme="@style/LoginPage"
            />
        <activity android:name=".StepsActivity" />
        <activity android:name=".NewContactActivity" />
        <activity android:name=".NewEventActivity" />
        <activity android:name=".DetailEventActivity" />
        <activity android:name=".CategoryDishes" />
        <activity android:name=".ui.inventory.InventoryConfirmActivity" />
        <activity
            android:name=".ContactsActivity"
            android:theme="@style/LoginPage" />
        <activity
            android:name=".EventsActivity"
            android:theme="@style/LoginPage" />
        <activity
            android:name=".DetailRecipeActivity"
            android:theme="@style/LoginPage" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/LoginPage" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>